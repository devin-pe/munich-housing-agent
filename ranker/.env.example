# Ranker pipeline secrets. Copy to the repo-root .env (shared with the daily agent).
# Needed by features.py for geocoding (address -> lat/lng) before the metro lookup.
# Enable "Geocoding API" (and "Routes API", already used by the daily agent).
GOOGLE_MAPS_API_KEY=
