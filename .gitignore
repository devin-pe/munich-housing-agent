# Secrets
.env

# Runtime data (dedup store, geocode/route caches, logs, digest previews).
# In CI the dedup state (data/seen.json) is persisted on a separate `agent-state`
# git branch by the workflow — not tracked on main — so main stays clean.
data/
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Scratch HTML dumps
*.html
