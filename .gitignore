# Secrets
.env

# Runtime data (geocode/route caches, logs, digest previews, old sqlite store)
# — but DO track data/seen.json, the dedup state, so it persists across CI runs.
data/*
!data/seen.json
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Scratch HTML dumps
*.html
